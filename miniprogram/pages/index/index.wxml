<van-card wx:for="{{productList}}" wx:key="{{index}}"
  num="{{item.productInventory}}"
  price="{{item.productPrice}}"
  desc="{{item.productContent}}"
  title="{{item.productName}}"
  thumb="{{ item.fileIds[0] }}"
/>

